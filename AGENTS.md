# Zolai-AI — Main (Management) Repo

This is the **main management repo** of the [Zolai-AI](https://github.com/Zolai-AI) org.
It holds the org-level context, documentation, and backlog — NOT the heavy source/data
(those live in the component repos). See `context/` for the org seven-file set.

## Relation to the workspace
The local workspace at `/home/peter/Documents/Projects/zolai-ai/` coordinates the repos.
This repo mirrors that management layer for GitHub.
